<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_book_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_book_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoreL07"
        tools:targetApi="31">
        <activity
            android:name=".activities.activity_view_profile"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegistrationActivity"
            android:exported="true" />

        <activity
            android:name=".activities.ProfileActivity"
            android:exported="true" />
        <activity
            android:name=".activities.CategoryActivity"
            android:exported="true" />
        <activity
            android:name=".activities.MyOrder"
            android:exported="true" />
        <activity
            android:name=".activities.ViewAllActivity"
            android:exported="true" />
        <activity
            android:name=".activities.DetailedActivity"
            android:exported="true" />
        <activity
            android:name=".activities.MyCartsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.NavCategoryActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PleacedOrderActivity"
            android:exported="true" />
    </application>

</manifest>